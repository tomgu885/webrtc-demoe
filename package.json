{
  "name": "webrtc-demo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "ENV=developement nodemon --env-file=.env.developement server.js",
    "build": "vite build",
    "preview": "vite preview",
    "prod": "ENV=production node --env-file=.env.production server.js"
  },
  "devDependencies": {
    "prettier": "^3.3.1",
    "vite": "^5.1.6"
  },
  "dependencies": {
    "express": "^4.21.1",
    "http": "^0.0.1-security",
    "https": "^1.0.0",
    "mkcert": "^3.2.0",
    "socket.io": "^4.8.1"
  }
}
